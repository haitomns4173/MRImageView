<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MRImageViewer" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorBlue</item>
        <item name="colorPrimaryVariant">@color/colorBlue</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->

    </style>

    <style name="Theme.MRImageViewer.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MRImageViewer.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.MRImageViewer.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="cardBackground">
        <item name="android:src">@drawable/rectangle</item>
    </style>
    <style name="labelColor">
        <item name="android:textColor">@color/colorPinkDark</item>
    </style>
    <style name="menuIconColor">
        <item name="android:color">@color/black</item>
    </style>
    <style name="backgroundColor">
        <item name="android:background">@color/backgroundLight</item>
    </style>
</resources>